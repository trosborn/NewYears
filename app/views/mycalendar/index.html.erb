<div class="row">
  <div class="large-8 columns">
    <%= calendar @events do |event| %>
      <div class="event"><%= link_to event.name, event %></div>
    <% end %>
  </div>
  <div class="large-4 columns">
    <br><br>
    <a href="<%= new_event_path %>" class="button round">Add goal record</a>
  </div>
</div>

